'use strict';
var lambdaPlugin = require('../');
var should = require('should');

describe('gulp-awslambda', function() {
	it('should do stuff correctly', function() {
		[1,2,3].indexOf(5).should.equal(-1);
	})
});
